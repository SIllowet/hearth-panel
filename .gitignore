# Holds your playit agent secret and local server paths — never commit this.
config.json

# Runtime junk
panel.log
__pycache__/
*.pyc

# In case anyone keeps a server folder inside the repo
servers/
backups/
*.jar
